# model weights and runtime example dir (large / local-only)
models/
images/
# NOTE: sample_images/ is committed on purpose — it seeds the demo examples
#       baked into the Azure container, so it must NOT be ignored.

# python
__pycache__/
*.pyc
.venv/
venv/
env/

# os
.DS_Store
